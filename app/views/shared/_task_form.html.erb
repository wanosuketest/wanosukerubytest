<%= form_for(@task) do |f| %>
<%= render 'shared/error_messages', object: f.object %>
	<div class="field">
		<%= f.label :content %>
		<%= f.text_area :content, placeholder: "タスクを入力してください"%>
	</div>
	<%= f.submit "登録する", class: "btn btn-primary" %>
<% end %>