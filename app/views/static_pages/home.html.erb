<% if logged_in? %>
	<div class="row">
		<aside class="col-md-4">
			<section class="user_info">
			</section>
			<section class="micropost_form">
				<%= render :partial => 'shared/task_form' , :locals => { :object => @user} %>
			</section>
		</aside>
		<div class="col-md-8">
			<h3>Task Feed</h3>
			<%= render 'shared/feed' %>
		</div>
	</div>
<% end %>